import { StyleSheet } from "react-native";

const buttonStyle = StyleSheet.create({
    numbers: {
        backgroundColor: '#f36e08',
        borderRadius: 100,
        textAlign: 'center',
        fontSize: 20,
        fontWeight: '200'
    }
})

export { buttonStyle };
